interface:
  display_name: "Set up engineering context"
  short_description: "Discover repository commands, architecture, and engineering boundaries."
  default_prompt: "Use $setup-engineering-context to inspect this repository and establish verified engineering context before making changes."
policy:
  allow_implicit_invocation: false
