interface:
  display_name: "Implement a verified change"
  short_description: "Implement one coherent slice at a time with recorded evidence."
  default_prompt: "Use $implement-change to implement the approved change one verified slice at a time while preserving authorization and rollback boundaries."
policy:
  allow_implicit_invocation: false
